import { Button } from "./Button";

export const Hero = {
  label: "Hero",
  name: "Hero",
  type: "object",
  fields: [
    {
      label: "Title",
      name: "title",
      type: "string",
    },
    Button,
  ],
};
